rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Default deny for MVP - app uses local assets exclusively
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
